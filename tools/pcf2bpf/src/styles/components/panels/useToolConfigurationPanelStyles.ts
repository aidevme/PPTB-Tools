import { makeStyles } from "@fluentui/react-components";

export const useToolConfigurationPanelStyles = makeStyles({
    body: {
        display: "flex",
        flexDirection: "column",
        gap: "12px",
    },
});
