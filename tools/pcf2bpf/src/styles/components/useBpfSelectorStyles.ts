import { makeStyles } from "@fluentui/react-components";

export const useBpfSelectorStyles = makeStyles({
    column: {
        display: "flex",
        flexDirection: "column",
        gap: "8px",
    },
    fullWidth: {
        width: "100%",
    },
});
