/** Which of `ScopeCard`'s pickers is active — a solution, a publisher, or neither
 * ("No Filtering", which loads every BPF in the environment unfiltered). */
export type FilterMode = "solutions" | "publishers" | "noFiltering";
