/** A Dataverse `solution` record, for the "Solutions & Publishers" filters. */
export interface SolutionInfo {
    solutionid: string;
    friendlyname: string;
    uniquename: string;
    version: string;
    description: string;
}
